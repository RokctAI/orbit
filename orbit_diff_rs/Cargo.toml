[package]
name = "orbit_diff_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "orbit_diff_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
similar = { version = "2.4.0", features = ["inline"] }
